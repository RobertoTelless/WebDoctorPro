@using Presentation.Extensions
@model ERP_Condominios_Solution.ViewModels.AreaPacienteViewModel
@{
    Layout = "~/Views/Shared/_Layout_Admin.cshtml";
    ViewBag.Title = "WebDoctor - Paciente - Envio";
    ViewBag.MetaDescription = "WebDoctor - Paciente - Enviar e-mail ao paciente";
    ViewBag.MetaKeywords = "WebDoctor, paciente, e-mail";
}
<div class="wrapper wrapper-content animated fadeInRight">
    <div class="page-title">
        <ol class="breadcrumb">
            <li>
                <a href="@Url.Action("MontarTelaAreaPaciente", "AreaPaciente")">Dashboard</a>
            </li>
            <li class="active">
                <strong>Envio de Documentos</strong>
            </li>
        </ol>
        <br />
    </div>

    <div class="row">
        <div class="col-lg-12 col-md-12 col-xs-12">
            <div class="col-lg-12 col-md-12 col-xs-12 ibox float-e-margins">
                <div class="ibox-title">
                    <div style="text-align: center;" class="col-lg-12 col-md-12 col-xs-12">
                        <h5>Envio de Documentos</h5>
                        <span style="color:green; font-weight: 500; font-size: 20px;">@ViewBag.NomePaciente</span>
                    </div>
                </div>
                @using (Html.BeginForm("MontarTelaEnvioDocumento", "AreaPaciente", new { ReturnUrl = ViewBag.ReturnUrl }, FormMethod.Post, new { @class = "form-horizontal", @id="pwd-container1" }))
                {
                    @Html.AntiForgeryToken()
                    <div class="ibox-content">
                        <div class="row">
                            <div class="form-group" style=" margin-left: -15px; margin-right: -15px;">                
                                <div class="col-lg-3 col-md-3 col-xs-12">
                                    @Html.HiddenFor(m => m.AREA_DT_CONSULTA)
                                    @Html.HiddenFor(m => m.AREA_DT_ENTRADA)
                                    @Html.HiddenFor(m => m.AREA_HR_FINAL)
                                    @Html.HiddenFor(m => m.AREA_HR_INICIO)
                                    @Html.HiddenFor(m => m.AREA_IN_ATIVO)
                                    @Html.HiddenFor(m => m.AREA_IN_TIPO)
                                    @Html.HiddenFor(m => m.AREA_NM_TITULO)
                                    @Html.HiddenFor(m => m.ASSI_CD_ID)
                                    @Html.HiddenFor(m => m.PACI_CD_ID)
                                    @Html.HiddenFor(m => m.USUA_CD_ID)
                                    @Html.HiddenFor(m => m.AREA_GU_IDENTIFICADOR)
                                    @Html.HiddenFor(m => m.NOME_PACIENTE)
                                    @Html.HiddenFor(m => m.NOME_PROFISSIONAL)
                                    @Html.HiddenFor(m => m.EMAIL_PROFISSIONAL)
                                    @Html.HiddenFor(m => m.HORARIO)
                                    Nome do Profissional
                                    @Html.TextBoxFor(m => m.NOME_PROFISSIONAL, new { @class = "form-control", @readonly="readonly", @style = "color: green; font-weight: bold" })
                                </div>
                            </div>
                            <div class="row">
                                <div class="col-lg-6 col-md-6 col-xs-12">
                                    <div class="form-group" style=" margin-left: -15px; margin-right: -15px;">                
                                        <h4 style="margin-left: 0px; color:green; background-color: beige; font-weight:bold">Informações do Documento</h4>
                                        <div class="col-lg-4 col-md-4 col-xs-12">
                                            Tipo de Documento*
                                            @Html.DropDownListFor(m => m.AREA_IN_TIPO_EXAME, (SelectList)ViewBag.Tipo, "Selecionar", new { @class = "form-control", data_toggle = "popover", data_content = "Selecione o tipo de documento a ser enviado", data_trigger="hover", data_placement="top", autofocus = true, @id = "tipo", onchange = "ValidaTipo($(this).val())"  })
                                            @Html.ValidationMessageFor(m => m.AREA_IN_TIPO_EXAME, "", new { @style = "color:red" })
                                        </div>
                                        <div class="col-lg-8 col-md-8 col-xs-12">
                                            Nome do Documento*
                                            @Html.TextBoxFor(m => m.AREA_NM_EXAME, new { @class = "form-control", autofocus = true, @id = "nomeDoc", data_toggle = "popover", data_content = "Informe o nome do documento a ser enviado", data_trigger="hover", data_placement="top" })
                                            @Html.ValidationMessageFor(m => m.AREA_NM_EXAME, "", new { @style = "color:red" })
                                        </div>
                                    </div>
                                    <div class="form-group" style=" margin-left: -15px; margin-right: -15px;"  id="dadosEx">                
                                        <h4 style="margin-left: 0px; color:green; background-color: beige; font-weight:bold">Informações do Exame</h4>
                                        <div class="col-lg-4 col-md-4 col-xs-12">
                                            Tipo de Exame*
                                            @Html.DropDownListFor(m => m.TIEX_CD_ID, (SelectList)ViewBag.TipoExame, "Selecionar", new { @class = "form-control", data_toggle = "popover", data_content = "Selecione o tipo de exame a ser enviado", data_trigger="hover", data_placement="top", autofocus = true, @id = "tipoExame"  })
                                            @Html.ValidationMessageFor(m => m.TIEX_CD_ID, "", new { @style = "color:red" })
                                        </div>
                                        <div class="col-lg-3 col-md-3 col-xs-12">
                                            Data do Exame
                                            @Html.TextBoxFor(m => m.AREA_DT_DATA_EXAME, "{0:dd/MM/yyyy}", new { @class = "form-control date-picker", autofocus = true, @id = "dn", data_toggle = "popover", data_content = "Informe a data de realização do exame a ser enviado", data_trigger="hover", data_placement="top" })
                                            @Html.ValidationMessageFor(m => m.AREA_DT_DATA_EXAME, "", new { @style = "color:red" })
                                        </div>
                                        <div class="col-lg-4 col-md-4 col-xs-12">
                                            Laboratório
                                            @Html.DropDownListFor(m => m.LABS_CD_ID, (SelectList)ViewBag.Labs, "Selecionar", new { @class = "form-control", data_toggle = "popover", data_content = "Selecione o laboratório onde o exame a ser enviado foi executado", data_trigger="hover", data_placement="top", autofocus = true, @id = "tipoExame"  })
                                            @Html.ValidationMessageFor(m => m.LABS_CD_ID, "", new { @style = "color:red" })
                                        </div>
                                    </div>
                                    <div class="form-group" style=" margin-left: -15px; margin-right: -15px;"  id="dadosLoca">                
                                        <h4 style="margin-left: 0px; color:green; background-color: beige; font-weight:bold">Informações da Locação</h4>
                                        <div class="col-lg-8 col-md-8 col-xs-12">
                                            Locação*
                                            @Html.DropDownListFor(m => m.LOCA_CD_ID, (SelectList)ViewBag.Locacao, "Selecionar", new { @class = "form-control", data_toggle = "popover", data_content = "Selecione a locação referente ao documento a ser enviado", data_trigger="hover", data_placement="top", autofocus = true, @id = "loca", onchange = "GetLocacao($(this).val())"   })
                                            @Html.ValidationMessageFor(m => m.LOCA_CD_ID, "", new { @style = "color:red" })
                                        </div>
                                        <div style="display: none;" id="situacao" class="col-lg-4 col-md-4 col-xs-12">
                                            Situação
                                            <input class="form-control" type="text" readonly="readonly" tabindex="-1"/>
                                        </div>
                                    </div>
                                    <div class="form-group" style=" margin-left: -15px; margin-right: -15px;">                
                                        <div class="col-lg-12 col-md-12 col-xs-12">
                                            Informações do documento sendo enviado*
                                            @Html.TextAreaFor(m => m.AREA_TX_CONTEUDO, new { @class = "form-control", style = "width: 100%; height: 400px; overflow:auto;", autofocus = true, @id = "texto" })
                                            @Html.ValidationMessageFor(m => m.AREA_TX_CONTEUDO, "", new { @style = "color:red" })
                                        </div>
                                    </div>
                                </div>

                                <div class="col-lg-6 col-md-6 col-xs-12">
                                    <h4 style="margin-left: -15px; color:green; background-color: beige; font-weight:bold">Anexos</h4>
                                    <input class="btn btn-sm btn-warning" style="margin-left: -15px;" id="inputFile" type="file" multiple data-toggle="popover" data-trigger="hover" data-placement="top" data-content="ATENÇÃO - Antes de anexar arquivos certifique-se de que eles estejam livres de vírus e outras ameaças.  "/>
                                    <br />
                                    <div class="form-group">
                                        <div>
                                            <hr />
                                            <table style="width: 100%;" class="table table-striped table-bordered table-hover dataTables-example" id="myFiles">
                                                <thead style="background-color:lightsteelblue">
                                                    <tr>
                                                        <td>Nome</td>
                                                        <td>Tipo</td>
                                                        <td>Ações</td>
                                                    </tr>
                                                </thead>
                                                <tbody></tbody>
                                            </table>
                                        </div>
                                    </div>
                                </div>
                            </div>
                        </div>

                        <div class="form-group">
                            @Html.ButtonActionSubmit(text: "BtnSubmit", htmlAttributes: new { id = "submit", style = "display: none;" })
                            <button style="margin-left: 15px;" class="btnSubmit btn btn-sm btn-success btn-loading1" id="basictoast" type="button">Confirmar Envio do Documento</button>
                            @Html.ButtonLinkIndex(text: "Voltar", action: "MontarTelaAreaPaciente", controller: "Paciente", htmlAttributes: new { @class = "btn-loading" })
                            <span id="loadingIndicator" style="display: none; margin-left: 10px; color: #007bff;">
                                <i class="fa fa-spinner fa-spin"></i> Processando...
                            </span>
                        </div>
                    </div>
                }
            </div>
         </div>
    </div>
</div>

@section Styles {
    <link href="~/Content/themes/base/jquery-ui.min.css" rel="stylesheet" />
    <link href="~/Content/themes/base/all.css" rel="stylesheet" />
    @Styles.Render("~/Content/plugins/dataTables/dataTablesStyles")
    @Styles.Render("~/plugins/jasnyBootstrapStyles")
    @Styles.Render("~/plugins/toastrStyles")
}
@section Scripts {
    <script type="text/javascript" src="~/Scripts/moment.min.js"></script>
    <script type="text/javascript" src="~/Scripts/bootstrap.min.js"></script>
    @Scripts.Render("~/plugins/toastr")
    <script src="~/Scripts/jquery-ui-1.12.1.min.js"></script>
    @Scripts.Render("~/plugins/dataTables")
    @Scripts.Render("~/bundles/inputmask")
    @Scripts.Render("~/plugins/jasnyBootstrap")
    <script src="https://cdnjs.cloudflare.com/ajax/libs/jquery.maskedinput/1.4.1/jquery.maskedinput.js"></script>    
    <script src="~/Scripts/uploadAnexo.js"></script>

    <script>
        SendFilesV2('inputFile', 'AreaPaciente', 'UploadFileToSession');
    </script>

        <script type="text/javascript">
        $(document).ready(function () { 
            $('.btn-loading').on('click', function (e) {            
                $(this).prop('disabled', true);
                $(this).addClass('disabled');
                $('#loadingIndicator').show();
                var tagName = $(this).prop('tagName').toLowerCase();
                if (tagName === 'a') {
                    e.preventDefault(); 
                    var targetUrl = $(this).attr('href');
                    window.location.href = targetUrl;

                } else if (tagName === 'button' || tagName === 'input') {
                    $(this).closest('form').submit();
                }
            });
        });
    </script>

    <script type="text/javascript">
        $(document).ready(function () {
            $('.btn-loading1').on('click', function (e) {
            
                var $this = $(this);
                var tagName = $this.prop('tagName').toLowerCase();
                $this.addClass('disabled'); 
                $('#loadingIndicator').show();

                if (tagName === 'a') {
                    e.preventDefault();
                
                    var targetUrl = $this.attr('href');
                    window.location.href = targetUrl;
                
                } else if (tagName === 'button' || tagName === 'input') {               
                    $this.closest('form').submit();
                    $this.prop('disabled', true);
                }
            });
        });
    </script>

    <script type="text/javascript">
        function ValidaTipo(valNac) {
            if (valNac == '3') {
                $("#dadosEx").show();
                $("#dadosLoca").hide();
            } else if (valNac == '4') {
                $("#dadosEx").hide();
                $("#dadosLoca").hide();
            } else {
                $("#dadosEx").hide();
                $("#dadosLoca").show();
            }
        }
        $(document).ready(function () {
            ValidaTipo($('#tipo').val());
        });
    </script>

    <script>
        function GetLocacao(id) {
            var data = {
                id: id
            }
            var solicitacao = $('select#loca');

            $.ajax({
                url: '../Locacao/GetLocacaoArea'
                , type: 'POST'
                , data: data
                , success: function (r) {
                    $('#situacao > input').val(r.situacao);
                    $('#situacao').show();
                }
            });
        }
    </script>

    <script type="text/javascript">
        $(document).ready(function () {

            var i = -1;
            var toastCount = 0;
            var $toastlast;
            var getMessage = function () {
                var msg = 'Hi, welcome to Inspinia. This is example of Toastr notification box.';
                return msg;
            };

            $('#basictoast').click(function () {
                // Display a success toast, with a title
                toastr.success('Envio em andamento!')
            });
            $('#showtoast').click(function () {
                var shortCutFunction = $("#toastTypeGroup input:radio:checked").val();
                var msg = $('#message').val();
                var title = $('#title').val() || '';
                var $showDuration = $('#showDuration');
                var $hideDuration = $('#hideDuration');
                var $timeOut = $('#timeOut');
                var $extendedTimeOut = $('#extendedTimeOut');
                var $showEasing = $('#showEasing');
                var $hideEasing = $('#hideEasing');
                var $showMethod = $('#showMethod');
                var $hideMethod = $('#hideMethod');
                var toastIndex = toastCount++;
                toastr.options = {
                    closeButton: $('#closeButton').prop('checked'),
                    debug: $('#debugInfo').prop('checked'),
                    progressBar: $('#progressBar').prop('checked'),
                    positionClass: $('#positionGroup input:radio:checked').val() || 'toast-top-right',
                    onclick: null
                };
                if ($('#addBehaviorOnToastClick').prop('checked')) {
                    toastr.options.onclick = function () {
                        alert('You can perform some custom action after a toast goes away');
                    };
                }
                if ($showDuration.val().length) {
                    toastr.options.showDuration = $showDuration.val();
                }
                if ($hideDuration.val().length) {
                    toastr.options.hideDuration = $hideDuration.val();
                }
                if ($timeOut.val().length) {
                    toastr.options.timeOut = $timeOut.val();
                }
                if ($extendedTimeOut.val().length) {
                    toastr.options.extendedTimeOut = $extendedTimeOut.val();
                }
                if ($showEasing.val().length) {
                    toastr.options.showEasing = $showEasing.val();
                }
                if ($hideEasing.val().length) {
                    toastr.options.hideEasing = $hideEasing.val();
                }
                if ($showMethod.val().length) {
                    toastr.options.showMethod = $showMethod.val();
                }
                if ($hideMethod.val().length) {
                    toastr.options.hideMethod = $hideMethod.val();
                }
                if (!msg) {
                    msg = getMessage();
                }
                $("#toastrOptions").text("Command: toastr["
                        + shortCutFunction
                        + "](\""
                        + msg
                        + (title ? "\", \"" + title : '')
                        + "\")\n\ntoastr.options = "
                        + JSON.stringify(toastr.options, null, 2)
                );
                var $toast = toastr[shortCutFunction](msg, title); // Wire up an event handler to a button in the toast, if it exists
                $toastlast = $toast;
                if ($toast.find('#okBtn').length) {
                    $toast.delegate('#okBtn', 'click', function () {
                        alert('you clicked me. i was toast #' + toastIndex + '. goodbye!');
                        $toast.remove();
                    });
                }
                if ($toast.find('#surpriseBtn').length) {
                    $toast.delegate('#surpriseBtn', 'click', function () {
                        alert('Surprise! you clicked me. i was toast #' + toastIndex + '. You could perform an action here.');
                    });
                }
            });
            function getLastToast() {
                return $toastlast;
            }
            $('#clearlasttoast').click(function () {
                toastr.clear(getLastToast());
            });
            $('#cleartoasts').click(function () {
                toastr.clear();
            });

        });
    </script>

    @if (ViewBag.Message != null)
    {
        <script type="text/javascript">
            window.onload = function () {
                  alert("@ViewBag.Message");
            };
        </script>
    }
}




